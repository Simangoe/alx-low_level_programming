#include <stdio.h>
#include <string.h>
#include <ctype.h>

char *_strpbrk(char *s, char *accept)
{
int i;
int strlenofs = strlen(s);

for (i = 0; i <= strlenofs; i++)
{
if (s[i] == accept[i])
{
return (&s[i - 1]);
break;
}
}
}
