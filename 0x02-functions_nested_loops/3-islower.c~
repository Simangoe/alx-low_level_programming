#include <stdio.h>
#include "main.h"
#include <ctype.h>
/**
 * print_alphabet_x10 - function to print alphabet in lower 10 times
 * Return: returns 0 (success)
 */
int _islower(int c)
{
_islower(c)
return (0);
}
