#include <stdio.h>
#include "main.h"
#include <ctype.h>
/**
 * _islower - function to print alphabet in lower 10 times
 * Return: returns 0 (success)
 */
int _islower(int c)
{
if (_islower(c))
{
return(1);
}
else
{
return (0);
}
 return (0);
}

