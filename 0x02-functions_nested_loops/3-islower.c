#include <stdio.h>
#include "main.h"
#include <ctype.h>
/**
 * _islower - function to print alphabet in lower 10 times
 * Return: returns 0 (success)
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
return(1);
else
return (0);
}

