#include <stdio.h>
/**
 * main - enter function to decide number sign
 * Return: return 0
 */
int main(void)
{
  char i = 'z';

for (i = 'z' ; i <= 'z' && i >= 'a'; i--)  
putchar(i);
putchar('\n');
return (0);
}
