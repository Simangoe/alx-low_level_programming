#include <ctype.h>
#include <stdio.h>
#include "main.h"
/**
 * mul - multiply two integers
 * @c: Parameter for the function to check
 * Return: returns multiple of a and b
 */
int mul(int a, int b)
{
int ret = a * b;
return (ret);
}
